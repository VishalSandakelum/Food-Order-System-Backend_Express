import { LitElement, TemplateResult, nothing, PropertyValueMap } from 'lit';
import { MemberType } from '../types/members.types';
import { InfiniteQueryController } from '../controllers/infinite-query-controller';
import { WeavyContextProps } from '../types/weavy.types';
import { ShadowPartsController } from '../controllers/shadow-parts-controller';
import { InfiniteQueryResultType } from '../types/query.types';
export default class WyUsersSearch extends LitElement {
    static styles: import('lit').CSSResult[];
    protected exportParts: ShadowPartsController;
    private weavyContext?;
    appId?: number;
    buttonTitle?: string;
    botFilter?: boolean;
    private selected;
    private select;
    text: string;
    peopleQuery: InfiniteQueryController<InfiniteQueryResultType<MemberType>>;
    private inputRef;
    private infiniteScroll;
    private pagerRef;
    private dispatchSubmit;
    private isChecked;
    private handleSelected;
    private clear;
    private throttledSearch;
    getSelected(): TemplateResult<1> | typeof nothing;
    getSearchResult(): TemplateResult[];
    render(): TemplateResult<1>;
    protected updated(changedProperties: PropertyValueMap<this & WeavyContextProps>): Promise<void>;
}
//# sourceMappingURL=wy-users-search.d.ts.map